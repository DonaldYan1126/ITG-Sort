<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Sort.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sort"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Sort">

    <Grid>
        <TextBlock x:Name="ConfigStatus" Margin="5"/>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ListView x:Name="FilesListView">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding FileName}" VerticalAlignment="Center"/>

                            <ComboBox Grid.Column="1" x:Name="Combo" 
                              ItemsSource="{Binding ElementName=FilesListView, Path=DataContext.Subjects}"
                              SelectedItem="{Binding TargetSubject, Mode=TwoWay}"
                              DisplayMemberPath="Name"/>

                            <Button Grid.Column="2" 
                            Content="Move File" 
                            Click="MoveButton_Click"
                            Tag="{Binding}"
                            IsEnabled="{Binding CanMove}"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Move all the files" Click="MoveAllButton_Click" Margin="5"/>
                <Button Content="Automatically sort" Click="AutoSortButton_Click" Margin="5"/>
            </StackPanel>

        </Grid>


        <Border x:Name="AppTitleBar" Grid.Column="1" VerticalAlignment="Top">
            <TextBlock x:Name="AppTitle" VerticalAlignment="Top" Margin="16,8,0,0" FontSize="12"/>
        </Border>

    </Grid>
</Window>
